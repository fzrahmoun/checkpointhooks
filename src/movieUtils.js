// export const getMovieDetailsById = (movies, movieId) => {
//     console.log('Movie utils:', movies,movieId,movies.description);
//     return movies.find((movie) => movie.id === parseInt(movieId));

//   };